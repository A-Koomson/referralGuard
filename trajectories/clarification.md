# ClarificationAgent

**Mode:** MOCK / deterministic trigger

When referral reason is empty, creates clarification:

> What is the clinical reason for this emergency referral?

Does **not** fill the missing value. Status remains open until clinician responds via API.

**Human checkpoint:** yes — clinician response required
